The database created with CODE FIRST 2.
The initial SQL script is in the solution folder.
For correct performance the connection string must be changed, the database will be created automatically with initial data.
All bugs are features(really).